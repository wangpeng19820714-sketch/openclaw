# mem0-openclaw

Thin OpenClaw bridge for a separately deployed upstream Mem0 service.

- OpenClaw plugin code lives here.
- Upstream `mem0ai/mem0` is deployed outside this repository.
- OpenClaw talks to Mem0 over HTTP via `baseUrl`.
